[project]
name = "dailyagentpapers"
version = "0.2.1"
description = "Daily arxiv Agent paper pipeline with LLM scoring"
requires-python = ">=3.13"
dependencies = [
    "pyyaml>=6.0",
    "arxiv>=2.1.0",
    "arxiv-to-prompt>=0.11.0",
    "pymupdf>=1.24.0",
    "python-dotenv>=1.0.0",
]
